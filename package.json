{
  "name": "mimo-pool",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "start": "node dist/index.js",
    "debug": "node --env-file=.env.debug dist/index.js",
    "build": "tsc -p tsconfig.json",
    "litellm:config": "npm run build && node dist/litellm-config.js",
    "litellm:start": "npm run build && node dist/litellm-start.js",
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "npm run build && node --test tests/e2e/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
