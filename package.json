{
  "name": "mimo-pool",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "start": "node dist/index.js",
    "debug": "node --env-file=.env.debug dist/index.js",
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
